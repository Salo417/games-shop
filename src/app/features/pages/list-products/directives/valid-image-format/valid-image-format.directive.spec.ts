import { ImageFormatValidator } from './valid-image-format.directive';

describe('ValidImageFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFormatValidator();
    expect(directive).toBeTruthy();
  });
});
